const namespace = "AUTH";

export const LOGIN_USER = `${namespace}/LOGIN`;
export const LOGOUT_USER = `${namespace}/LOGOUT`;

export const UPDATE_USER = `${namespace}/UPDATE_USER`;
export const UPDATE_TOKEN = `${namespace}/UPDATE_TOKEN`;
